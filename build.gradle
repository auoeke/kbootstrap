plugins {
    id("wheel-fabric").version("latest.integration")
    id("fabric.mod.gradle").version("latest.integration")
}

group = "net.auoeke"
version = "0.0.0"

sourceSets {
    main {
        java.srcDir("src")
    }
}

mod {
    languageAdapter("kbootstrap", "net.auoeke.kbootstrap.KBootstrap")
}
