pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()

        maven {url = "https://maven.fabricmc.net"}
        maven {url = "https://maven.auoeke.net"}
    }
}

rootProject.name = "kbootstrap"

includeBuild("plugin")

include(":testmod")
