plugins {
    id("wheel-fabric")
    id("fabric.mod.gradle")
    id("org.jetbrains.kotlin.jvm").version("latest.integration")
}

group = "net.auoeke"
version = "1.2.3"

sourceSets {
    main {
        kotlin.srcDir("src")
    }
}

compileKotlin {
    kotlinOptions.jvmTarget = targetCompatibility
}

repositories {
    maven {url = "https://maven.auoeke.net"}
}

dependencies {
    implementation(rootProject)
    implementation("net.auoeke:fabric-mod-gradle:latest.integration")
    compileOnly("org.jetbrains.kotlin:kotlin-stdlib:latest.integration")
}

mod {}
